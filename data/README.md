# Data Directory

The raw dataset CSV files (`Books.csv`, `Ratings.csv`, `Users.csv`) are downloaded directly from Kaggle using `kagglehub`.

To download the dataset files into this directory, run:
```bash
python download_data.py
```

Or generate a lightweight synthetic sample for testing:
```bash
python sample_data_generator.py
```
